#include <bits/stdc++.h>
using namespace std;
int main(){
    vector<string> v;
    string s;
    while(cin>>s) {
        v.push_back(s);
        v[s]++;
    }
    mp
    for(auto i : mp) cout << i.first << " - " << i.second << endl;
}