#include<bits/stdc++.h>
using namespace std;
int main(){
    vector<pair<pair<string,string>,int > > v;
    int n; cin >> n;
    while(n--){
        string s;
        int x; cin >> x;
        v.push_back(s,t,xS);
    }
    for(int i=0; i<v; )
}